[project]
name = "sprint-3"
version = "0.1.0"
description = "ChargeGrid Intelligence / NexusCharge — Sprint 3 do EV Challenge 2026 (FIAP + GoodWe)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
